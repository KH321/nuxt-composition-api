import Vue from 'vue'
import { setupVM } from '~/hooks/setupVM'

const hooksPlugin = () => {
  setupVM(new Vue())
  // Vue.mixin({
  //   beforeCreate () {
  //   }
  // })
}

export default hooksPlugin
